This directory is reserved for Flask-Migrate revision files.

Create the migration history locally with:

1. `flask db init` (first run only)
2. `flask db migrate -m "initial schema"`
3. `flask db upgrade`
